version: 1

formatters:
  simple:
    format: "[%(levelname)s] %(asctime)s - %(name)s: %(message)s"

handlers:
  console:
    class: logging.StreamHandler
    formatter: simple
    level: INFO

loggers:
  rag_service:
    handlers: [console]
    level: INFO
    propagate: False