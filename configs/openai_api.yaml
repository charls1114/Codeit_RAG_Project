profile: "openai_api"
rag_mode: "openai_api"

llm:
  model_name: "gpt-5-mini"
  temperature: 0.0
  max_new_tokens: 512
  # api_key는 .env(OPENAI_API_KEY)에서 주입

embeddings:
  model_name: "text-embedding-3-small"
  # api_key는 .env(OPENAI_API_KEY)에서 주입
